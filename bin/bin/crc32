#!/bin/zsh

# Calculate CRC32 but output in base 16

pyprog=$(cat << 'EOF'
try:
    while True:
        num, size, name = input().split(maxsplit=2)
        print(f"{int(num):08X} {size} {name}")
except EOFError:
    pass
EOF
)

cksum $@ | python -c "$pyprog"
